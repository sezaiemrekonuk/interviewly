{
  "name": "@interviewly/worker",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "cd .. && eslint --config eslint.config.js worker/src"
  },
  "dependencies": {
    "pino": "^9",
    "zod": "^3"
  }
}
